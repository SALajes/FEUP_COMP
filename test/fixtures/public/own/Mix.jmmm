class Mix {

	boolean b;
	int repeatedVar;
    int repeatedVar;
	int[] p;

	public static void main(String[] args) {

        int i;
        int[] array;
        boolean testBool;
        UnknownClass var;
        Mix mix;

        mix = new Mix();

        notDeclared = 5;
        i = 1;
        i = true;

        //initialize array
        array = new int[true];
        array[1] = 1;
        testBool = true && false && testBool;
        testBool = 1 < 1;

        b = i && true;
        b = 1 && p;
        b = true && true;

        i.nonExistentMethod();

        //test length property
        repeatedVar=p.length;
        repeatedVar=b.length;
        repeatedVar=testBool;
        repeatedVar=array.length;

        var.call();

        //test !
        b = !1;
        b = !true;
        b = !array;

        //while section
        while(i < 5){
          b=false;
        }

        while(p){
          b = true;
        }

        //if section
        if(true && false && testBool){
          b;
        } else {
          b = !false;
        }

        if(array){
          b = true;
        } else {
          b;
        }

        if(i){
          b = !b;
        } else {
          b;
        }

        var = new Mix();
        var.test(i, i);

        i = array[true];
        i = array;
        i = array[1];
        //i = mix.non_existing_array().length + 1;

        this.non_existing_array();

        1 + true;
        1 < true;
        1 && false;
        1 && 3;

        false && true;
        3 < 4;

        i[1] = 0;

        //test parameters
        mix.getAcount();
        mix.getAcount(i);
        mix.getAcount(testBool , 10);

    	}

  public int getAccount() {
    int i;
    int a;

    i = 1;
    a = 40;

    return i * a;
  }

  public int[] non_existing_array() {
      return array;
  }

  public int badReturn() {

    boolean bool;

    return true && bool;
  }


}