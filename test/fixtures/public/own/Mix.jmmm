class Mix {

	boolean b;
	int repeatedVar;
    int repeatedVar;
	int[] p;

	public static void main(String[] args) {

        int i;
        int[] array;
        boolean testBool;
        UnknownClass var; //error 1
        Mix mix;

        mix = new Mix();

        notDeclared = 5;//error 2
        i = 1;
        i = true;//error 3

        //initialize array
        array = new int[true]; //error 4
        array[1] = 1;
        testBool = true && false && testBool;
        testBool = 1 < 1;

        b = i && true;//error 5
        b = 1 && p; //error 6 e 7
        b = true && true;


        i.nonExistentMethod(); //error 8 e 9

        //test length property
        repeatedVar=p.length;
        repeatedVar=b.length; //error 10 e 11
        repeatedVar=testBool; //error 12
        repeatedVar=array.length;

        var.call(); //error 13 e 14

        //test !
        b = !1;//error 15
        b = !true;
        b = !array;//error 16
        b = !testBool;
        b = !i;//error 17

        //while section
        while(i < 5){
          b=false;
        }

        while(p){//error 18
          b = true;
        }

        //if section
        if(true && false && testBool){
          b;
        } else {
          b = !false;
        }

        if(array){//error 19
          b = true;
        } else {
          b;
        }

        if(i){//error 20
          b = !b;
        } else {
          b;
        }

        var = new Mix();//error 21
        var.test(i, i);//error 22 e 23

        i = array[true];//error 24
        i = array;//error 25
        i = array[1];

        this.non_existing_array();

        1 + true; //error 26
        1 < true; //error 27
        1 && false; //error 28
        1 && 3; //error 29 e 30

        false && true;
        3 < 4;

        i[1] = 0;//error 31

        //test parameters
        mix.getAccount();
        mix.getAccount(i);//error 23
        mix.getAccount(testBool , 10);//error 24


    }

    public int getAccount() {
        int i;
        int a;

        i = 1;
        a = 40;

        return i * a;
    }

    public int[] non_existing_array() {
        return array;
    }

    public int badReturn() {

        boolean bool;

        return true && bool;//error 25
    }

    public boolean testInitialize(){
        boolean test;
        boolean lol;

        test = true;

        if(true){
            test = false;
        }else{
            lol = true;
        }

        return true;
    }
}