//This test is supposed to succeed

class Arithmetic {

  int[] array;
  int a;
  Arithmetic arithmetic;

	public static void main(String[] args) {
		int b;
        boolean bool;

		b=0;
        bool=true;


        array = new int[5];

        true && false;
        false && true;
        15+0;
        7 < 14;

        b+1;
        b+b;
        7/8+2*6+1/7;
        bool && bool && bool;

        arithmetic = new Arithmetic();
        (arithmetic).returnArray();

        b = 0;

        while(48 + 1 < b ) {
            a = array[9];
            b = b + 1;
        }

        if (bool) {
            b = 1;
        } else {
                b = 2;
        }

        if(bool && true){
            b = 0;
        }else{
            b  = 1 + 2 * 3 / 4 -5 / 7;
        }

	}

	public int[] returnArray() {
        int[] value;

        value = new int[5];
        value[1] = 9;

        return value;
    }
}