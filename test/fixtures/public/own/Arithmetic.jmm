//This test is supposed to succeed

import static ioPlus.printResult(int) void;
import static io.println(int) void;

class Arithmetic {

    int[] array;
    int a;

    public static void main(String[] args) {
        int size;
        int index;
        int result;
        Arithmetic arithmetic;

        arithmetic = new Arithmetic();
        size = 5;
        index = 3;

        if(index < size){
            result = arithmetic.expressions(size, index);
        }
        else{
            result = 0-1;
        }

        ioPlus.printResult(result);
    }

    public int expressions(int size, int index){
        int b;
        boolean bool;

        b=0;
        bool=true;

        array = new int[size];

        while(b < size){
            array[b] = 0;
            b = b + 1;
        }

        bool = true && false;
        bool = false && true;
        b = 15+0;
        bool = 7 < 14;
        b = b+1;
        b = b+b;
        b = 7/8+2*6+1/7;
        bool = bool && bool && bool;

        this.returnArray();

        array[index] = b * 666 * 999;

        b = 0;

        while(48 + 1 < b ) {
            a = array[index];
            b = b + 1;
        }

        if (bool) {
            b = 1;
        } else {
            b = 2;
        }

        if(bool){
            b = 0;
        }else{
            b  = 1 + 2 * 3 / 4 -5 / 7;
        }

        return array[index];
    }

    public int[] returnArray() {
        int[] value;

        value = new int[3];
        value[1] = 666;

        io.println(value[1]);

        return value;
    }
}